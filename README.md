This is my first time creating this project in order to improve my coding knowledge.

This is the _"1st phase"_ of the project and there will be a _"2nd phase"_ soon, which will incorporate GUI coding into the project. This 1st project contained the __if/else__ statements, and mostly used a __button__ function instead of the __prompt__ because it might be a little tedious to some players which you need to type your choice.

Before starting this project, I mostly reading the previous lectures first and understanding them again, and also get some ideas from a separate source (__Google__) in order to make the project more functional. This will further improve my knowledge of __JavaScript__ more.

Learning Javascript is harder and more complicated to understand than HTML and CSS, which is why I do the __JavaScript__ file first before __HTML__ and __CSS__, it took some time, but I managed to finished the _JavaScript_ file before proceeding to the __HTML__ file in oder to test my codes. I proceed to __CSS__ to style my project and it took some time because of all the grouping in html and doing some adjustments to the stylings, but I managed to finished my project.

Although doing __JavaScript__ might be intimidating at times, but it is actually very _rewarding_ once you understand the concept of the code itself.

