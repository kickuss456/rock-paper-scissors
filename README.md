This is my first time creating this project in order to improve my coding knowledge.

This is the _"1st phase"_ of the project and there will be a _"2nd phase"_ soon, which will incorporate GUI coding into the project. This 1st project contained the _if/else_ statements, and mostly used a _button_ function instead of the _prompt_ because it might be a little tedious to some players which you need to type your choice.

Before starting this project, I mostly reading the previous lectures first and understanding them again, and also get some ideas from a separate source(google) in order to make the project more functional. This will further
improve my knowledge of _JavaScript_ more.

Learning Javascript is harder and more complicated to understand than HTML and CSS, which is why I do the _JavaScript_ file first before HTML and CSS, it took some time, but I managed to finished the _JavaScript_ file before proceeding to the _HTML_ file in oder to test my codes. I proceed to _CSS_ to style my project and it took some time because of all the grouping in html and doing some adjustments to the stylings, but I managed to finished my project.

Although doing JavaScript might be intimidating at times, but it is actually very _rewarding_ once you understand the concept of the code itself.

